version: 0.2

env:
  git-credential-helper: yes
phases:
  install:
    runtime-versions:
      nodejs: 10
  config:
    commands:
      - ls -lt
      - npm install -g serverless
      - npm install -D
  test:
    commands:
      - npm run test